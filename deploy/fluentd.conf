<source>
  @type forward
</source>

<filter foo.bar>
  @type grep
  <regexp>
    key mykey
    pattern /In the future, we may add more patterns for convenience of log aggregation./
  </regexp>
</filter>

<match **>
  @type stdout
</match>

